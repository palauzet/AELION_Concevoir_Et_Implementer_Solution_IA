[project]
name = "indusense"
version = "0.1.0"
description = "InduSense 4.0 — Sprint 01 : enquête et nettoyage de données (maintenance prédictive)"
readme = "README.md"
requires-python = ">=3.13,<3.14"
license = { text = "Proprietary" }
authors = [{ name = "Patrick Alauzet" }]

# Dépendances Sprint 01 (US 1.1 -> 1.5 : exploration, anonymisation,
# intégration SQLAlchemy, outliers/imputation, time-series split, PCA).
dependencies = [
    "pandas>=2.2",
    "numpy>=2.1",
    "scipy>=1.14",
    "scikit-learn>=1.5",
    "sqlalchemy>=2.0",
    "alembic>=1.13",         # migrations / rollback du schéma (US 1.2)
    "psycopg[binary]>=3.2",  # driver PostgreSQL (psycopg 3, binaire embarqué)
    "matplotlib>=3.9",
    "seaborn>=0.13",
    "pyarrow>=17.0",      # Gold Dataset au format parquet
    "python-dotenv>=1.0",
    "mlflow>=2.15",   # tracking des runs d'entraînement (backend SQLite local)
    "xgboost>=2.1",   # gradient boosting — modèle principal du POC ML
    "optuna>=4.0",    # optimisation d'hyperparamètres (B7, backend SQLite local)
    "shap>=0.46",     # explicabilité des modèles (B8)
]

[project.scripts]
indusense-ingest = "indusense.data.ingest:main"
indusense-incidents = "indusense.data.incidents:main"
indusense-telemetry = "indusense.data.telemetry:main"
indusense-referentiel = "indusense.data.referentiel:main"
indusense-schema = "indusense.data.schema:main"
indusense-silver = "indusense.data.silver:main"
indusense-gold = "indusense.data.gold:main"
indusense-train = "indusense.ml.training:main"
indusense-tune = "indusense.ml.tuning:main"
indusense-explain = "indusense.ml.explain:main"

[dependency-groups]
# Outillage de dev — installé via `uv sync` (groupe activé par défaut).
dev = [
    "jupyterlab>=4.2",
    "ipykernel>=6.29",
    "pytest>=8.3",
    "ruff>=0.6",
    "mypy>=1.11",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/indusense"]

[tool.ruff]
line-length = 100
target-version = "py313"
src = ["src", "tests"]
extend-exclude = ["alembic"]  # migrations autogénérées + env Alembic (style propre à Alembic)

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.mypy]
python_version = "3.13"
packages = ["indusense"]
ignore_missing_imports = true

[tool.pyright]
include = ["src", "tests"]
typeCheckingMode = "basic"
# Bruit de stubs tiers (mlflow expose ses sous-modules dynamiquement ; pandas/scikit-learn
# stubs élargissent les types de retour en unions Series|DataFrame|ndarray) sans rapport avec
# un bug réel — le vérificateur faisant foi pour ce projet est `mypy` (cf. [tool.mypy]
# ci-dessus, propre sur tout le code), qui ne remonte aucun de ces cas.
reportPrivateImportUsage = false
reportArgumentType = false
reportReturnType = false
reportCallIssue = false
reportAttributeAccessIssue = false
reportGeneralTypeIssues = false
reportOperatorIssue = false
