"""Sous-package ml : entraînement et comparaison de modèles (TP B5).

- ``training`` : chargement du gold, pipelines sklearn (imputation/scaling/encodage
  fit-train), 3 modèles (régression logistique, Random Forest, XGBoost), évaluation
  (PR-AUC prioritaire), validation croisée temporelle, tracking MLflow.
"""
